apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "net-tools.fullname" . }}-test-connection"
  labels:
    {{- include "net-tools.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "net-tools.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
